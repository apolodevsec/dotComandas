# Ticket 12: Pacote @dotcomandas/ui e Tokens Universais Haute Hospitality

Status: resolved

## Descrição
Criar o pacote compartilhado `packages/ui` no monorepo e exportar `tokens.ts` e `variables.css` com as definições do `design.md`.

## Tarefas
- [ ] Inicializar a estrutura de `packages/ui` no pnpm workspace.
- [ ] Exportar `tokens.ts` com tokens universais em TypeScript (cores, tipografia, espaçamentos, geometria rectilinear `roundedness: 0`).
- [ ] Exportar `variables.css` para consumo fácil em Next.js / Web.
- [ ] Adicionar testes unitários para validar os tokens de design.
