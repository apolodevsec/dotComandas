# 01: Monorepo Scaffold & Shared Types

**What to build:** Scaffold the project workspace containing `apps/web` (Next.js), `apps/mobile` (React Native Expo), `apps/api` (Node.js REST/WebSocket), and `packages/shared` with domain TypeScript definitions for Comanda, Mesa, Pedido, Item do Cardápio, and State Machines.

**Blocked by:** None (can start immediately)

**Status:** resolved

- [x] Configure pnpm workspace root and package structure
- [x] Create `packages/shared` with canonical domain types and state machine contracts
- [x] Create basic app entrypoints for `apps/api`, `apps/web`, and `apps/mobile`
