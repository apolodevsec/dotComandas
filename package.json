{
  "name": "dotcomandas",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run",
    "dev": "pnpm -r --parallel dev"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^1.2.0"
  },
  "dependencies": {
    "@types/jsonwebtoken": "^9.0.10",
    "jsonwebtoken": "^9.0.3"
  }
}
